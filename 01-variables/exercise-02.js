// const vs let
const cat = {
    name: 'Shimako',
    sex: 'female',
};

//changing a property inside the object
cat.name = 'Ema'

let place;
//reassinging a variable
place = 'Sanchaco';

console.log(place);
console.log(cat);